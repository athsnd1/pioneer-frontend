import { ClipLoader } from "react-spinners"

export default function Loading() {
  
    
    return (
        <div className="flex items-center justify-center mt-75">
            <ClipLoader size={30} color="var(--main-color)"/>
        </div>)
}
